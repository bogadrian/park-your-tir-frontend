import React from 'react';
import { connect } from 'react-redux';

import './place.scss';

const Place = ({ ...props }) => {
  return (
    <div>
      <h1>Place Page</h1>
    </div>
  );
};

export default Place;
